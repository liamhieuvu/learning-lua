--[[
Exercise 1.2: Run the twice example, both by loading the file with the -l
option and with dofile. Which way do you prefer?
--]]

function twice (x)
	return 2.0 * x
end

print("dofile is preferred")
